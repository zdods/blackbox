// Test stub for SvelteKit's $app/navigation, aliased in vitest.config.js so
// modules that import `goto` can be unit-tested outside the SvelteKit runtime.
// vitest's vi.spyOn can wrap this in tests that assert navigation occurred.
export function goto() {}
