# Optional: override in production
# JWT_SECRET=your-secret-key

# For local blackbox-server (without Docker), if not set defaults are used:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/blackbox?sslmode=disable
# SERVER_ADDR=:8080
# STATIC_DIR=web/build

# TLS (optional): enable HTTPS and WSS. Use wss:// and https:// for agents and browser.
# TLS_CERT_FILE=/path/to/cert.pem
# TLS_KEY_FILE=/path/to/key.pem
